# 复制为 .dev.vars 后填入真实值；.dev.vars 已被 git 忽略。
# Wrangler 在 `npm run dev` 时会把下列键值注入为 Worker 的环境变量 (c.env.*)。
# 生产环境请改用 `wrangler secret put <KEY>` 注入，不要提交任何真实密钥。
JWT_SECRET=replace-with-a-long-random-string
AI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
AI_BASE_URL=https://api.dgrid.ai/v1
AI_MODEL=anthropic/claude-haiku-4.5
# Google OAuth Web Client ID（需要和前端 VITE_GOOGLE_CLIENT_ID 保持一致）
GOOGLE_CLIENT_ID=xxxxxxxxxxx.apps.googleusercontent.com
