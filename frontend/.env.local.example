# 复制为 .env.local 后填入真实值；.env.local 已被 git 忽略。
# 这三个 AI_* 变量只在本地 `npm run dev` 的 vite AI 代理中读取（见 vite.config.ts）。
# 生产环境的 AI 调用走 Cloudflare Worker，由 `wrangler secret put` 注入。
AI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
AI_BASE_URL=https://api.dgrid.ai/v1
AI_MODEL=anthropic/claude-haiku-4.5

# Google OAuth Web Client ID（可选；不填时使用代码里的默认值）。
# VITE_ 前缀的变量会被打进前端 bundle，Client ID 本身不是秘密（Google 设计如此）。
VITE_GOOGLE_CLIENT_ID=xxxxxxxxxxx.apps.googleusercontent.com
